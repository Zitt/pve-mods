diff --git a/usr/share/pve-manager/js/pvemanagerlib.js b/../pvemanagerlib.js
index 116e97a7..65e4f710 100644
--- a/usr/share/pve-manager/js/pvemanagerlib.js
+++ b/../pvemanagerlib.js
@@ -44998,7 +45021,7 @@ Ext.define('PVE.node.StatusView', {
     extend: 'Proxmox.panel.StatusView',
     alias: 'widget.pveNodeStatus',
 
-    height: 520,
+    height: 660,
     bodyPadding: '15 5 15 5',
 
     layout: {
@@ -45079,97 +45102,147 @@ Ext.define('PVE.node.StatusView', {
 	    maxField: 'swap',
 	    renderer: Proxmox.Utils.render_node_size_usage,
 	},
+	{
+	    iconCls: 'fa fa-fw fa-hdd-o',
+	    itemId: 'nvme0n1',
+	    title: '/ ' + gettext('HD Life'),
+	    valueField: 'nvme0n1',
+	    maxField: 'nvme0n1',
+		printBar: true,
+	    renderer: Proxmox.Utils.render_node_nvme_life,
+	},
+	{
+	    xtype: 'box',
+	    printBar: false,
+		itemId: 'dummy2',
+	    colspan: 1,
+		textField: '',
+	    padding: '0 10 10 10',
+	},
+	{
+		itemId: 'nvme2temp',
+		iconCls: 'fa fa-fw fa-thermometer-half',
+		title: gettext('NVMe2 temp'),
+		valueField: 'nvme2temp',
+		maxField: 'nvme2temp',
+		renderer: Proxmox.Utils.render_node_temp,
+	},
 	{
 	    xtype: 'box',
 	    colspan: 2,
 	    padding: '0 0 20 0',
 	},
 	{
-		    itemId: 'cputemp',
-		    iconCls: 'fa fa-fw fa-thermometer-half',
-		    title: gettext('CPU temp'),
-		    valueField: 'cputemp',
-		    maxField: 'cputemp',
-		    renderer: Proxmox.Utils.render_node_temp,
+		itemId: 'cputemp',
+		iconCls: 'fa fa-fw fa-thermometer-half',
+		title: gettext('CPU temp'),
+		valueField: 'cputemp',
+		maxField: 'cputemp',
+		renderer: Proxmox.Utils.render_node_temp,
 	},
 	{
-		    itemId: 'nvme2temp',
-		    iconCls: 'fa fa-fw fa-thermometer-half',
-		    title: gettext('NVMe2 temp'),
-		    valueField: 'nvme2temp',
-		    maxField: 'nvme2temp',
-		    renderer: Proxmox.Utils.render_node_temp,
+		itemId: 'cputemp2',
+		iconCls: 'fa fa-fw fa-thermometer-half',
+		title: gettext('CPU temp2'),
+		valueField: 'cputemp2',
+		maxField: 'cputemp2',
+		renderer: Proxmox.Utils.render_node_temp,
 	},
 	{
-		    itemId: 'cputemp2',
-		    iconCls: 'fa fa-fw fa-thermometer-half',
-		    title: gettext('CPU temp2'),
-		    valueField: 'cputemp2',
-		    maxField: 'cputemp2',
-		    renderer: Proxmox.Utils.render_node_temp,
+		itemId: 'systemp',
+		iconCls: 'fa fa-fw fa-thermometer-half',
+		title: gettext('Mobo temp'),
+		valueField: 'systemp',
+		maxField: 'systemp',
+		renderer: Proxmox.Utils.render_node_temp,
 	},
 	{
-		    itemId: 'systemp',
-		    iconCls: 'fa fa-fw fa-thermometer-half',
-		    title: gettext('Mobo temp'),
-		    valueField: 'systemp',
-		    maxField: 'systemp',
-		    renderer: Proxmox.Utils.render_node_temp,
+		itemId: 'pico',
+		iconCls: 'fa fa-fw fa-thermometer-half',
+		title: gettext('picoFan die'),
+		valueField: 'picoTemp',
+		maxField: 'picoTemp',
+		renderer: Proxmox.Utils.render_node_temp,
 	},
 	{
-		    xtype: 'box',
-		    colspan: 2,
-		    padding: '0 0 20 0',
+		itemId: 'inlet',
+		iconCls: 'fa fa-fw fa-thermometer-half',
+		title: gettext('temp In'),
+		valueField: 'inletTemp',
+		maxField: 'inletTemp',
+		renderer: Proxmox.Utils.render_node_temp,
 	},
 	{
-		    itemId: 'fan1rpm',
-		    iconCls: 'fa fa-fw fa-cog fa-spin',
-		    title: gettext('Fan1'),
-		    valueField: 'fan1rpm',
-		    maxField: '1200',
-			printBar: false,
-		    renderer: Proxmox.Utils.render_node_fan,
+		itemId: 'outlet',
+		iconCls: 'fa fa-fw fa-thermometer-half',
+		title: gettext('temp Out (near CPU exhaust)'),
+		valueField: 'outletTemp',
+		maxField: 'outletTemp',
+		renderer: Proxmox.Utils.render_node_temp,
 	},
 	{
-		    itemId: 'fan2rpm',
-		    iconCls: 'fa fa-fw fa-cog fa-spin',
-		    title: gettext('Fan2'),
-		    valueField: 'fan2rpm',
-		    maxField: '2400',
-			printBar: false,
-		    renderer: Proxmox.Utils.render_node_fan,
+		xtype: 'box',
+		colspan: 2,
+		padding: '0 0 20 0',
 	},
 	{
-		    itemId: 'fan4rpm',
-		    iconCls: 'fa fa-fw fa-cog fa-spin',
-		    title: gettext('Fan4'),
-		    valueField: 'fan4rpm',
-		    maxField: '2000',
-			printBar: false,
-		    renderer: Proxmox.Utils.render_node_fan,
+		itemId: 'fan1rpm',
+		iconCls: 'fa fa-fw fa-cog fa-spin',
+		title: gettext('Fan1'),
+		valueField: 'fan1rpm',
+		maxField: 'fan1rpm',
+		printBar: true,
+		renderer: Proxmox.Utils.render_node_fan,
 	},
 	{
-		    itemId: 'fan6rpm',
-		    iconCls: 'fa fa-fw fa-cog fa-spin',
-		    title: gettext('Fan6'),
-		    valueField: 'fan6rpm',
-		    maxField: 6000,
-			printBar: false,
-		    renderer: Proxmox.Utils.render_node_fan,
+		itemId: 'fan2rpm',
+		iconCls: 'fa fa-fw fa-cog fa-spin',
+		title: gettext('Fan2'),
+		valueField: 'fan2rpm',
+		maxField: 'fan2rpm',
+		printBar: true,
+		renderer: Proxmox.Utils.render_node_fan,
 	},
 	{
-		    itemId: 'fan7rpm',
-		    iconCls: 'fa fa-fw fa-cog fa-spin',
-		    title: gettext('Fan7'),
-		    valueField: 'fan7rpm',
-		    maxField: 3000,
-			printBar: false,
-		    renderer: Proxmox.Utils.render_node_fan,
+		itemId: 'fan3rpm',
+		iconCls: 'fa fa-fw fa-cog fa-spin',
+		title: gettext('Fan3'),
+		valueField: 'fan3rpm',
+		maxField: 'fan3rpm',
+		printBar: true,
+		renderer: Proxmox.Utils.render_node_fan,
 	},
 	{
-		    xtype: 'box',
-		    colspan: 2,
-		    padding: '0 0 20 0',
+		itemId: 'fan4rpm',
+		iconCls: 'fa fa-fw fa-cog fa-spin',
+		title: gettext('Fan4'),
+		valueField: 'fan4rpm',
+		maxField: 'fan4rpm',
+		printBar: true,
+		renderer: Proxmox.Utils.render_node_fan,
+	},
+	{
+		itemId: 'fan6rpm',
+		iconCls: 'fa fa-fw fa-cog fa-spin',
+		title: gettext('Fan6'),
+		valueField: 'fan6rpm',
+		maxField: 'fan6rpm',
+		printBar: true,
+		renderer: Proxmox.Utils.render_node_fan,
+	},
+	{
+		itemId: 'fan7rpm',
+		iconCls: 'fa fa-fw fa-cog fa-spin',
+		title: gettext('Fan7'),
+		valueField: 'fan7rpm',
+		maxField: 'fan7rpm',
+		printBar: true,
+		renderer: Proxmox.Utils.render_node_fan,
+	},
+	{
+		xtype: 'box',
+		colspan: 2,
+		padding: '0 0 20 0',
 	},
 	{
 	    itemId: 'cpus',
 	    colspan: 2,

